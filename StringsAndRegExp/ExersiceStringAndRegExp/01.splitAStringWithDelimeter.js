function solve(tex,delimeter){
    let splitEl  = tex.split(delimeter);

    for (let i = 0; i < tex.length-1; i++) {

        console.log(splitEl[i]);
    }



}solve("One-Two-Three-Four-Five", "-" )